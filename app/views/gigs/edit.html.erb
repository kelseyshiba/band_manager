<h1>Admin only</h1>
<div class="container">
    <div class='form-group'>

<%= form_for :gig, url: gig_path, method: :patch do |f| %>
<%= render partial: 'sessions/errors', locals: {obj: @gig}%>
<%= render partial: 'form', locals: {f: f} %>
<% end %>

    </div>
</div>